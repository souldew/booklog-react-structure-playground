# .env.local にコピーして使う。値はローカル起動の既定と同じ。
# サーバー側 (Server Component / Server Action) から api を呼ぶときの base URL
API_BASE_URL=http://localhost:8787
# ブラウザから api を直接呼ぶときの base URL
NEXT_PUBLIC_API_BASE_URL=http://localhost:8787
